{
  "name": "seenit-e2e",
  "private": true,
  "scripts": {
    "test": "node --test",
    "smoke": "bddgen && playwright test --config playwright.config.js",
    "report": "node scripts/trim-videos.js allure-results 3 && allure generate allure-results --clean -o allure-report && allure open allure-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "allure-commandline": "^2.43.0",
    "allure-playwright": "^3.10.2",
    "playwright-bdd": "^9.2.0"
  }
}
