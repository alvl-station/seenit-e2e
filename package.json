{
  "name": "seenit-e2e",
  "private": true,
  "scripts": {
    "test": "node --test",
    "smoke": "playwright test --config playwright.config.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0"
  }
}
